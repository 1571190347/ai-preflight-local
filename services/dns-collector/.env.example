# Replace every example value before starting. Never commit your real .env.
DNS_ZONE=probe.example.com
PUBLIC_IPV4=203.0.113.10
# Generate with: openssl rand -hex 32
DNS_COLLECTOR_TOKEN=REPLACE_WITH_AT_LEAST_32_RANDOM_URL_SAFE_CHARACTERS
API_BIND=127.0.0.1
API_PORT=8053
DNS_BIND=0.0.0.0
DNS_PORT=53
MAX_SESSIONS=1000
